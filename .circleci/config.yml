version: 2.1
jobs:
  rom:
    machine:
      image: ubuntu-2204:current
    steps:
      - checkout
      - run:
          name: "build"
          command: bash builder.sh
workflows:
  rom-workflow:
    jobs:
      - rom
